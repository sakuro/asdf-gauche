#!/usr/bin/env bash

set -eu -o pipefail

release_url="https://sourceforge.net/projects/gauche/files/Gauche/"

curl -sL "$release_url" |
grep -Eo 'Gauche-([0-9.]+).tgz' |
sed -e 's/^Gauche-//' -e 's/\.tgz$//' |
LC_ALL=C sort -t. -k1,1 -k2,2n -k 3,3n -k 4,4n |
uniq |
tr '\12' ' '
